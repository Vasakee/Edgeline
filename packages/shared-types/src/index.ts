/**
 * Placeholder interfaces for the edgeline trading agent.
 * These will be fully defined in the next phase.
 */

/** A snapshot of odds data for a specific market at a point in time. */
export interface OddsSnapshot {}

/** A trading opportunity identified by the agent. */
export interface Opportunity {}

/** An open or closed position held by the agent. */
export interface Position {}

/** A decision made by the autonomous agent. */
export interface AgentDecision {}
